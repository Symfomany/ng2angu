/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AgoPipe } from './ago.pipe';

describe('AgoPipe', () => {
  it('create an instance', () => {
    let pipe = new AgoPipe();
    expect(pipe).toBeTruthy();
  });
});
